$ANSIBLE_VAULT;1.1;AES256
PLACEHOLDER_ENCRYPTED_CONTENT
# This file should be encrypted with ansible-vault
# Run: ansible-vault create vault/secrets.yml
# or: ansible-vault encrypt vault/secrets.yml
#
# Expected content (unencrypted):
# ---
# vault_ansible_host: "YOUR_VM_IP"
# vault_ansible_user: "YOUR_SSH_USER"
# vault_ansible_ssh_key: "~/.ssh/YOUR_KEY"
# vault_telegram_bot_token: "YOUR_BOT_TOKEN"
# vault_telegram_allowed_users:
#   - "@BroAwn"
# vault_gateway_token: "YOUR_GATEWAY_TOKEN"
# vault_openai_api_key: "YOUR_OPENAI_API_KEY"