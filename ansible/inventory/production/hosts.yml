---
# Production Environment Inventory
# =====================================
# This inventory uses GCP dynamic inventory
# Static inventory for localhost (control node) only

all:
  children:
    localhost:
      hosts:
        127.0.0.1:
          ansible_connection: local
    
    production:
      children:
        openclaw_production:
      vars:
        environment: production
        env_short: prd