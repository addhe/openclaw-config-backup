---
# MasterControl Inventory
# Control plane bot - DM only, no group access
# Only accessible by BroAwn (Telegram ID: 319535690)

all:
  vars:
    ansible_user: addheputra
    ansible_ssh_private_key_file: ~/.ssh/mastercontrol-gcp
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
    ansible_become: true
    ansible_become_method: sudo
    ansible_become_user: root
  children:
    mastercontrol:
      hosts:
        mastercontrol-001-stg:
          ansible_host: 34.128.119.73
          deploy_env: staging
          role: mastercontrol
          worker_id: "001"